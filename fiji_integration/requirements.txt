# Myotube Segmentation - Fiji Integration Requirements
# Install with: pip install -r requirements.txt

# Core dependencies - CPU-only versions
--extra-index-url https://download.pytorch.org/whl/cpu
torch==1.10.2+cpu
torchvision==0.11.3+cpu
torchaudio==0.10.2+cpu

# Model dependencies
timm>=0.4.12  # PyTorch Image Models - required for Mask2Former

# Image processing
opencv-python>=4.5.0
numpy>=1.19.0,<2.0.0  # Pin to NumPy 1.x for PyTorch 1.10.2 compatibility
Pillow==9.4.0
scikit-image>=0.18.0

# Data handling
scipy>=1.5.0
pandas>=1.1.0

# Progress bar
tqdm>=4.50.0

# GUI (built-in with Python, but ensure tkinter is available)
# Note: tkinter usually comes with Python but may need: sudo apt-get install python3-tk (Linux)

# Config parsing
pyyaml>=5.3

# Utilities
matplotlib>=3.3.0
pycocotools>=2.0.0

# Optional: For faster inference
# ninja  # Uncomment if you want faster CUDA kernel compilation
